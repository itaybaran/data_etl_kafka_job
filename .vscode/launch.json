{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run utils.main",
      "type": "python",
      "request": "launch",

      // ✨ IMPORTANT: run as module, not script file
      "module": "utils.main",

      // make sure cwd is the project root
      "cwd": "${workspaceFolder}",

      // extra safety: ensure project root is on PYTHONPATH
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      },

      "justMyCode": true
    }
  ]
}
